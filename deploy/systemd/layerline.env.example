# Optional environment file loaded by deploy/systemd/layerline.service.
# Keep secrets out of git. Put production Cloudflare tokens or cert paths in
# /etc/layerline/layerline.env on the host.

# LAYERLINE_LOG_LEVEL=info
